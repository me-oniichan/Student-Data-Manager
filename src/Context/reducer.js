import { CURRENT_DATA, DATA_TO_UPDATE, SET_DATA, SET_LOADING } from "./action.type";

export default (state, action) =>{
    switch (action.type){
        case SET_DATA:
            return
        case SET_LOADING:
            return
        case DATA_TO_UPDATE:
            return
        case CURRENT_DATA:
            return
    }
}